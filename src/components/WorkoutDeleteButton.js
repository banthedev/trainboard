import { Button } from '@chakra-ui/react'

export default function WorkoutDeleteButton() {
    return (
        <Button colorScheme='red' size='lg'>
            Delete Workout
        </Button>
    )
}